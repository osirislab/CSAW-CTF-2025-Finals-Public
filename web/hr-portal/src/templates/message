<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied - HR Portal</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --black: #000000;
            --white: #ffffff;
            --gray-50: #fafafa;
            --gray-100: #f5f5f5;
            --gray-200: #e5e5e5;
            --gray-300: #d4d4d4;
            --gray-400: #a3a3a3;
            --gray-500: #737373;
            --gray-600: #525252;
            --gray-700: #404040;
            --gray-800: #262626;
            --gray-900: #171717;
            --red: #ef4444;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--white);
            color: var(--black);
            line-height: 1.6;
            font-size: 16px;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }
        
        .container {
            max-width: 480px;
            width: 100%;
            margin: 0 auto;
            text-align: center;
        }
        
        .error-icon {
            width: 80px;
            height: 80px;
            background: var(--red);
            border-radius: 50%;
            margin: 0 auto 3rem;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        
        .error-icon::before,
        .error-icon::after {
            content: '';
            position: absolute;
            width: 3px;
            height: 24px;
            background: var(--white);
            border-radius: 2px;
        }
        
        .error-icon::before {
            transform: rotate(45deg);
        }
        
        .error-icon::after {
            transform: rotate(-45deg);
        }
        
        .error-code {
            font-size: 1rem;
            color: var(--gray-500);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 1rem;
            font-weight: 500;
        }
        
        h1 {
            font-size: 2.5rem;
            font-weight: 300;
            letter-spacing: -0.02em;
            margin-bottom: 1rem;
            color: var(--black);
        }
        
        .message {
            font-size: 1.125rem;
            color: var(--gray-600);
            margin-bottom: 3rem;
            font-weight: 400;
            line-height: 1.5;
        }
        
        .details {
            background: var(--gray-50);
            border: 1px solid var(--gray-200);
            padding: 2rem;
            margin-bottom: 3rem;
            text-align: left;
        }
        
        .details-title {
            font-size: 1rem;
            font-weight: 500;
            color: var(--black);
            margin-bottom: 1rem;
            letter-spacing: -0.01em;
        }
        
        .details-list {
            list-style: none;
            padding: 0;
        }
        
        .details-list li {
            padding: 0.5rem 0;
            color: var(--gray-600);
            font-size: 0.9rem;
            border-bottom: 1px solid var(--gray-200);
        }
        
        .details-list li:last-child {
            border-bottom: none;
        }
        
        .details-list li strong {
            color: var(--black);
            font-weight: 500;
        }
        
        .actions {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            align-items: center;
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 1rem 2rem;
            text-decoration: none;
            color: var(--black);
            background: var(--white);
            border: 1px solid var(--gray-200);
            font-size: 1rem;
            font-weight: 500;
            letter-spacing: -0.01em;
            transition: all 0.15s ease;
            min-width: 200px;
        }
        
        .btn:hover {
            background: var(--gray-50);
            border-color: var(--gray-300);
        }
        
        .btn:active {
            background: var(--gray-100);
            transform: scale(0.995);
        }
        
        .btn-primary {
            background: var(--black);
            color: var(--white);
            border-color: var(--black);
        }
        
        .btn-primary:hover {
            background: var(--gray-800);
            border-color: var(--gray-800);
        }
        
        .btn-secondary {
            color: var(--gray-600);
        }
        
        .footer {
            margin-top: 4rem;
            padding-top: 2rem;
            border-top: 1px solid var(--gray-200);
        }
        
        .footer-text {
            font-size: 0.875rem;
            color: var(--gray-400);
            font-weight: 400;
        }
        
        .support-info {
            margin-top: 1rem;
            font-size: 0.875rem;
            color: var(--gray-500);
        }
        
        .support-info a {
            color: var(--black);
            text-decoration: underline;
            text-underline-offset: 2px;
        }
        
        /* Responsive */
        @media (max-width: 480px) {
            body {
                padding: 1.5rem;
            }
            
            .container {
                max-width: 100%;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            .error-icon {
                width: 60px;
                height: 60px;
                margin-bottom: 2rem;
            }
            
            .error-icon::before,
            .error-icon::after {
                width: 2px;
                height: 18px;
            }
            
            .details {
                padding: 1.5rem;
            }
            
            .btn {
                padding: 0.875rem 1.5rem;
                min-width: 180px;
            }
        }
        
        /* Animations */
        .container > * {
            animation: fadeInUp 0.4s ease forwards;
            opacity: 0;
            transform: translateY(10px);
        }
        
        .error-icon { animation-delay: 0.1s; }
        .error-code { animation-delay: 0.15s; }
        h1 { animation-delay: 0.2s; }
        .message { animation-delay: 0.25s; }
        .details { animation-delay: 0.3s; }
        .actions { animation-delay: 0.35s; }
        .footer { animation-delay: 0.4s; }
        
        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        /* Focus states */
        .btn:focus {
            outline: 2px solid var(--black);
            outline-offset: 2px;
        }
        
        /* Selection */
        ::selection {
            background: var(--black);
            color: var(--white);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-icon"></div>
        
        <div class="error-code">{{error}}</div>
        
        {% if error == '200' %} <h1>HTTP Success</h1> {% elif error == '403' %} <h1>Access Denied</h1> {% elif error == '404' %} <h1>Not Found</h1> {% else %} <h1>Unknown Error</h1> {% endif %}
        
        <p class="message">
            {{message | safe}}
        </p>
        
        <div class="details">
            <div class="details-title">Request Details</div>
            <ul class="details-list">
                <li><strong>Timestamp:</strong> <script>document.write(new Date().toLocaleString())</script></li>
                <li><strong>User:</strong> {{ user or 'Anonymous' }}</li>
                <li><strong>Resource:</strong> /admin</li>
                <li><strong>Required Level:</strong> Administrator</li>
            </ul>
        </div>
        <div class="footer">
            <p class="footer-text">If you believe this is an error, contact your system administrator.</p>
        </div>
    </div>
</body>
</html>